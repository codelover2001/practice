const { useState, useEffect, useRef } = React;

const SECTIONS = [
  { id: "structures", label: "Data Structures", icon: "🧱", color: "#E8B931" },
  { id: "architecture", label: "Architecture", icon: "🏗️", color: "#4A90D9" },
  { id: "microservices", label: "Microservices", icon: "🔌", color: "#50C878" },
  { id: "bigdata", label: "Big Data", icon: "📊", color: "#E74C3C" },
  { id: "observability", label: "Observability", icon: "🔍", color: "#9B59B6" },
  { id: "security", label: "Security", icon: "🔐", color: "#F39C12" },
];

const S = [
  // ============ DATA STRUCTURES ============
  { id:0, section:"structures", phase:"GEOHASH", title:"Geohash — Encoding Locations as Strings", icon:"📍", color:"#E8B931",
    concepts:["Geohash","Spatial Indexing","Proximity Search"],
    actors:["Restaurant Coordinates","Geohash Encoder","Nearby Search Query"],
    simple:"Your food app needs to answer: 'find all restaurants within 3km of Rahul's location.' Latitudes and longitudes are numbers like (12.9716, 77.5946). How do you query 'nearby' efficiently when you have 500,000 restaurants?\n\nGeohash converts a lat/lng coordinate into a SHORT STRING like 'tdr1y'. The clever part: locations that are PHYSICALLY CLOSE have SIMILAR geohash strings. So 'find nearby restaurants' becomes 'find all restaurants whose geohash starts with tdr1'. Just a string prefix search — blazingly fast.",
    detail:"HOW GEOHASH WORKS:\n\nThe Earth is divided into a grid. Each cell gets a code.\n\nStep 1: Split the world into 32 cells. Each cell gets a character (0-9, b-z).\n  World → 32 cells, each labeled: 0, 1, 2, ..., 9, b, c, ..., z\n\nStep 2: Split each cell into 32 smaller cells. Append another character.\n  'd' (one 32nd of the world) → 32 sub-cells → 'd0', 'd1', 'd2', ...\n\nStep 3: Keep subdividing. More characters = smaller area.\n\nGeohash precision (length → cell size):\n  1 char:  ~5,000 km (continent)\n  4 chars: ~40 km (city)\n  6 chars: ~1.2 km (neighborhood)\n  8 chars: ~38 m (building)\n  12 chars: ~4 cm (precise location)\n\nEXAMPLE — Meghana Foods in Bangalore:\n  Coordinates: (12.9716, 77.5946)\n  Geohash: 'tdr1ywnqhrs3' (12 chars)\n  \n  Truncate to 5 chars: 'tdr1y' (covers ~5 km area)\n  Truncate to 6 chars: 'tdr1yw' (covers ~1.2 km area)\n\nWHY NEARBY LOCATIONS SHARE PREFIXES:\n  Meghana Foods:    tdr1ywnq...\n  Nearby restaurant: tdr1ywn... ← same first 6 chars = within 1.2km!\n  Far away:         tdr3kpw... ← only first 3 chars match = different area\n\nFINDING NEARBY RESTAURANTS:\n  1. Compute Rahul's geohash: 'tdr1yw...'\n  2. Query database:\n     SELECT * FROM restaurants WHERE geohash LIKE 'tdr1y%'\n  3. Returns all restaurants in ~5km area.\n  4. For exact distance, calculate haversine formula on results.\n\nTHE EDGE CASE PROBLEM:\nRahul stands on the BOUNDARY between two cells.\nRestaurant 10 meters away might have a completely different geohash!\nSolution: also check the 8 neighboring cells (surrounding grid).\nLibraries handle this automatically.\n\nWHO USES GEOHASH:\n  — Uber: matching drivers to riders\n  — Redis GEO commands (internally)\n  — Elasticsearch geo queries\n  — Your food app: finding nearby restaurants\n\nYOUR FOOD APP:\n  When restaurants are added, compute and store their geohash.\n  Index on geohash column (B+ Tree for prefix search).\n  Nearby search: prefix match + haversine refinement.\n  Much faster than comparing distance to every restaurant!",
    analogy:"🍕 Imagine pizza delivery zones labeled on a map. Instead of calculating distance to every restaurant, the city is divided into numbered squares. Square '12' is sub-divided into 12A, 12B, 12C... each sub-divided again. Restaurants in square 12A3F are physically close to each other. To find 'restaurants near me,' just check: 'what square am I in? List all restaurants in that square.' The more characters in the square name, the smaller the area."
  },
  { id:1, section:"structures", phase:"TREES", title:"Quad Trees & R-Trees — Spatial Indexing", icon:"🌳", color:"#4A90D9",
    concepts:["Quad Tree","R-Tree","Spatial Index","2D Partitioning"],
    actors:["Map Region","Tree Root","Quadrants","Leaf Nodes"],
    simple:"Geohash is simple but has boundary issues. QUAD TREES and R-TREES are smarter ways to index 2D space (like maps).\n\nQUAD TREE: recursively divides a map into 4 quadrants (NW, NE, SW, SE). Each quadrant can be further divided into 4 more quadrants. When you query 'what's at location X?', you zoom down the tree to the right quadrant.\n\nR-TREE: groups nearby objects into bounding boxes. Boxes can overlap and nest. Used by most map databases (PostGIS uses R-Trees).",
    detail:"━━━ QUAD TREE ━━━\n\nStart with a map divided into 4 quadrants:\n  ┌─────┬─────┐\n  │ NW  │ NE  │\n  ├─────┼─────┤\n  │ SW  │ SE  │\n  └─────┴─────┘\n\nIf a quadrant has too many items (say 10+), subdivide it into 4 more:\n  NE quadrant → NE-NW, NE-NE, NE-SW, NE-SE\n\nKeep subdividing. Dense areas get many levels, sparse areas stay shallow.\n\nADVANTAGE: Adapts to data density.\n  Bangalore city (many restaurants) → deeply subdivided, small cells\n  Rural area (few restaurants) → shallow, large cells\n\nQUERY 'FIND ALL RESTAURANTS IN THIS REGION':\n  1. Start at root. Does search region overlap with quadrant?\n  2. If yes, recurse into that quadrant's 4 children.\n  3. At leaf level, check individual restaurants.\n  4. Skip quadrants that don't overlap. MASSIVE speedup.\n\nUSE CASE: Image compression, collision detection in games, static 2D data.\n\n━━━ R-TREE (better for real-world data) ━━━\n\nInstead of fixed quadrants, R-Trees use BOUNDING BOXES that adapt to where objects actually are.\n\nStart: 500,000 restaurants in India.\n\nLevel 1: Group into ~100 bounding boxes (each containing ~5000 restaurants).\n  Box 1: covers Bangalore area\n  Box 2: covers Mumbai area\n  Box 3: covers Delhi area\n  ... etc\n\nLevel 2: Within each box, group into ~100 smaller boxes.\n  Box 1 (Bangalore) → {HSR Layout box, Koramangala box, Whitefield box, ...}\n\nBoxes can OVERLAP. Objects only exist in leaf boxes.\n\nQUERY 'restaurants within 3km of Rahul':\n  1. Start at root. Check which top-level boxes intersect 3km radius.\n  2. Recurse only into overlapping boxes.\n  3. At leaves, return actual restaurants.\n  4. Logarithmic time instead of scanning 500,000 rows!\n\nWHY R-TREES WIN FOR REAL DATA:\n  Quad trees force fixed grid → inefficient if data is clustered unevenly.\n  R-trees adapt to data distribution → tight bounding boxes everywhere.\n\nWHO USES THEM:\n  PostGIS (PostgreSQL spatial extension): R-Trees\n  Google Maps tile system: Quad Trees\n  MongoDB geo indexes: internal R-Tree-like structure\n  Airbnb 'search this area' feature: R-Trees\n\nYOUR FOOD APP:\n  PostGIS R-Tree index on restaurant locations.\n  Query: 'restaurants in bounding box (lat1, lng1) to (lat2, lng2)' → instant.\n  Combined with your geohash approach for proximity search.",
    analogy:"🍕 Quad tree = dividing India into 4 quadrants. North India has lots of restaurants, so subdivide it into 4 more. Each dense city gets subdivided again. Rural areas stay as one big quadrant. R-tree = instead of forcing square quadrants, draw TIGHT boxes around actual clusters of restaurants. Bangalore's box fits exactly around Bangalore, Mumbai's fits around Mumbai. Much more efficient than forcing everything into equal squares."
  },
  { id:2, section:"structures", phase:"SKIP LIST", title:"Skip Lists — Fast Sorted Lookups", icon:"🪜", color:"#50C878",
    concepts:["Skip List","Probabilistic Data Structure","O(log n) Search"],
    actors:["Sorted Data","Multi-Level Links","Fast Search"],
    simple:"A sorted linked list lets you iterate through items in order, but finding a specific item takes O(n) — you must walk through every node. A Skip List adds 'express lanes' — additional forward links that skip over multiple nodes. This makes searches O(log n), same as a balanced tree, but much simpler to implement.\n\nRedis uses skip lists internally for its Sorted Sets (ZADD, ZRANGE). That's how ZADD and ZRANGE are so fast on millions of items.",
    detail:"THE PROBLEM:\n\nSorted linked list: 1 → 5 → 8 → 12 → 17 → 23 → 31 → 42 → 55 → 63 → 78\nFind 55? Walk from 1: 1, 5, 8, 12, 17, 23, 31, 42, 55. Found after 9 hops.\nWith 1 million items, that's 1 million hops worst case.\n\nSKIP LIST adds express lanes above the base list:\n\nLevel 3:  1 ────────────────────────────────────→ 78\nLevel 2:  1 ──────────→ 23 ──────────→ 55 ────→ 78\nLevel 1:  1 ───→ 12 ───→ 23 ───→ 42 ───→ 55 ───→ 78\nLevel 0:  1 → 5 → 8 → 12 → 17 → 23 → 31 → 42 → 55 → 63 → 78\n\nFIND 55:\n  Start at top level. Move right until you'd overshoot.\n  Level 3: 1 → 78 (overshoots 55, drop down)\n  Level 2: 1 → 23 → 55 FOUND!\n  Total: 3 hops instead of 9!\n\nWITH MILLIONS OF ITEMS: log₂(1,000,000) ≈ 20 hops. Blazing fast.\n\nHOW TO BUILD THE LEVELS:\n  For each node, flip a coin.\n  Heads → promote to next level.\n  Heads again → promote again.\n  Tails → stop promoting.\n  \n  So ~50% of nodes are in level 1, ~25% in level 2, ~12.5% in level 3, etc.\n  This is called PROBABILISTIC balancing.\n\nCOMPARED TO BALANCED TREES:\n                    Skip List         Red-Black Tree\n  Search:           O(log n)          O(log n)\n  Insert:           O(log n)          O(log n) + rotations\n  Implementation:   Simple            Complex (rotations!)\n  Concurrent:       Easy (lock-free)  Hard\n  Used by:          Redis, LevelDB    STL maps, Java TreeMap\n\nSkip lists are the 'lazy' data structure — simpler code, similar performance.\n\nYOUR FOOD APP (via Redis):\n  ZADD trending_restaurants 4.8 \"Meghana Foods\"\n  Internally, Redis inserts into a skip list ordered by score.\n  \n  ZRANGE trending_restaurants 0 9\n  Skip list walk → top 10 in O(log n + 10) time.\n  \n  ZRANK trending_restaurants \"Meghana Foods\"\n  Skip list search → O(log n).\n\nEvery leaderboard in your app uses skip lists under the hood.",
    analogy:"🍕 Imagine a long list of numbered restaurants sorted by rating. To find rating 4.7, you could walk through all 500,000. OR you could have express lanes: a 'top 100 list' that samples every 5000th restaurant, a 'top 10 list' that samples every 50,000th. To find rating 4.7, check the top 10 list first (narrows to a range), then top 100 (narrows more), then walk the last few. 3-4 jumps instead of 500,000. That's the skip list idea."
  },
  { id:3, section:"structures", phase:"MERKLE", title:"Merkle Trees — Verifying Data Integrity", icon:"🔒", color:"#9B59B6",
    concepts:["Merkle Tree","Hash Tree","Data Verification"],
    actors:["Data Blocks","Hash Functions","Merkle Root","Verification"],
    simple:"You have 1 million data items replicated on two servers. They should be identical. How do you quickly check if they're the same WITHOUT comparing all 1 million items?\n\nA Merkle Tree computes a single 'summary hash' from all the data. If the two servers have the same Merkle root hash → they have identical data. If the hashes differ, you can efficiently drill down to find EXACTLY which items differ.\n\nUsed by Git (to compare commits), Bitcoin (to verify blocks), Cassandra and DynamoDB (to sync replicas).",
    detail:"HOW A MERKLE TREE WORKS:\n\nStart with data blocks at the bottom:\n  Block 1: 'order_001'\n  Block 2: 'order_002'\n  Block 3: 'order_003'\n  Block 4: 'order_004'\n\nLEVEL 0 — Hash each block:\n  H1 = hash(Block1)  →  'a3f5...'\n  H2 = hash(Block2)  →  'b7c2...'\n  H3 = hash(Block3)  →  'd9e1...'\n  H4 = hash(Block4)  →  'f4a8...'\n\nLEVEL 1 — Hash pairs of hashes:\n  H12 = hash(H1 + H2) → '5e2b...'\n  H34 = hash(H3 + H4) → '8c4d...'\n\nLEVEL 2 — Hash the top level:\n  ROOT = hash(H12 + H34) → '7f9a...'\n\nThis single 'ROOT' hash summarizes ALL the data. Change ANY byte in any block → the root hash changes completely (cryptographic hash property).\n\nSYNCING TWO REPLICAS:\n\nReplica A root: 7f9a...\nReplica B root: 7f9a...\n→ Same! Data is identical. Done in ONE comparison.\n\nIf they differ:\nReplica A root: 7f9a...\nReplica B root: 3c1e... (different!)\n\nDRILL DOWN to find which block differs:\n  Compare H12: Same on both? If yes, Block 1 and 2 are fine.\n  Compare H34: Different!\n    Compare H3: Same → Block 3 OK.\n    Compare H4: Different → BLOCK 4 is the problem!\n\nWith 1 million blocks, comparing blocks one by one = 1M comparisons.\nWith Merkle tree, log₂(1M) ≈ 20 comparisons to find the differing block!\n\nWHY THIS MATTERS:\n  Cassandra has nodes A and B with millions of replicated rows.\n  Every hour, they compare Merkle roots. Same? No action needed.\n  Different? Drill down, find the few rows that differ, sync just those.\n  Anti-entropy repair in minutes instead of hours.\n\nBLOCKCHAIN USE:\n  Bitcoin block contains thousands of transactions.\n  Block header has the Merkle root of all transactions.\n  To prove 'transaction X is in block Y', you only need the Merkle path (few hashes), not the whole block.\n  This is called a 'Merkle proof' — compact and verifiable.\n\nGIT USE:\n  Every commit's content is summarized as a tree of hashes.\n  Two commits with same root hash = identical trees.\n  'git pull' compares hashes to know what to download.\n\nYOUR FOOD APP:\n  If you replicate order data across data centers, Merkle trees let you efficiently detect and sync drift.\n  CDN edge nodes use Merkle trees to sync cached content.\n  Database replica verification.",
    analogy:"🍕 Imagine two restaurants should have identical menus of 1000 items. Compare all 1000? Slow. Instead: take a fingerprint (hash) of each menu item. Then fingerprints of groups of 10 items. Then one master fingerprint of everything. If master fingerprints match → menus identical! If not, compare group fingerprints to find which group of 10 differs, then which item. Find 1 difference in log(1000) ≈ 10 comparisons."
  },
  { id:4, section:"structures", phase:"SKETCHES", title:"HyperLogLog & Count-Min Sketch", icon:"📉", color:"#E74C3C",
    concepts:["HyperLogLog","Count-Min Sketch","Probabilistic","Approximate"],
    actors:["Massive Data Stream","Tiny Memory","Approximate Answer"],
    simple:"Your food app gets 100 million unique users per month. How do you count UNIQUE visitors without storing 100 million user IDs (which would require gigabytes of RAM)?\n\nOr: how do you answer 'how many times was restaurant X searched today?' for millions of restaurants without tracking every search?\n\nPROBABILISTIC DATA STRUCTURES give you APPROXIMATE answers using TINY memory. HyperLogLog counts unique items in 12KB with 2% error. Count-Min Sketch tracks frequency of many items in a small table. They're magic tricks of computer science.",
    detail:"━━━ HYPERLOGLOG (HLL) — counting unique items ━━━\n\nTHE PROBLEM:\n  How many unique users visited today?\n  Naive: store every user ID in a Set. Check size.\n  100M unique users × 16 bytes = 1.6 GB of RAM. Expensive!\n\nHLL SOLUTION: 12KB for 100M unique users with ~2% error.\n\nHOW IT WORKS (intuition):\n  For each user ID, compute hash(id) → long binary string.\n  Count LEADING ZEROS of the binary string.\n  Example: 00001011... → 4 leading zeros.\n  \n  Track the MAXIMUM leading zeros seen.\n  If you saw 10 leading zeros once → probably saw ~2^10 = 1024 unique items.\n  \n  Why? If items are random, seeing 10 leading zeros has probability 1/1024.\n  So you probably saw about 1024 items to see one with 10 leading zeros.\n\nTo reduce variance, use many registers (1024 or more) and average.\nFinal estimate: ~2% error on billions of items.\n\nREDIS HLL:\n  PFADD unique_visitors user_123\n  PFADD unique_visitors user_456\n  PFADD unique_visitors user_123    ← duplicate, HLL handles it\n  PFCOUNT unique_visitors\n  → Returns ~2 (approximate count of unique items)\n\nYOUR FOOD APP:\n  Unique daily visitors per restaurant (millions of restaurants × millions of users)\n  Unique customers per zip code per month\n  Unique searches per category\n  All tracked with tiny memory footprint.\n\nPRO: Memory usage is CONSTANT regardless of count (1B or 1T, same 12KB).\nCON: Approximate, ~2% error. Not suitable for exact counts.\n\n━━━ COUNT-MIN SKETCH — tracking frequencies ━━━\n\nTHE PROBLEM:\n  How many times was each restaurant searched today?\n  Naive: HashMap with 10M restaurants as keys. Huge RAM.\n\nCOUNT-MIN SKETCH: fixed-size 2D array, tracks frequencies with some overcounting.\n\nStructure: Table with k rows × w columns (e.g., 4 rows × 1000 cols = 4000 counters total).\nUse k different hash functions (one per row).\n\nRECORDING 'restaurant_123 was searched':\n  For each row i, compute hash_i(restaurant_123) % w → column j.\n  Increment table[i][j].\n  Now 4 counters got +1 (one per row).\n\nQUERYING 'how many searches for restaurant_123?':\n  For each row i, find column hash_i(restaurant_123) % w → get count.\n  You get 4 counts. Return the MINIMUM.\n  \n  Why minimum? Each counter might be overcounted (different restaurants hash to same cell).\n  But the TRUE count can't be higher than the smallest counter.\n  So MIN is the least-overcounted estimate.\n\nWHY IT WORKS:\n  Collisions inflate counts UPWARD only. Never underestimates.\n  Multiple hash functions make all 4 overcounting the same item very unlikely.\n  Min of 4 values → close to true count.\n\nERROR: Overcounts by at most ε with probability 1-δ (configurable).\n  Bigger table → less overcounting → more memory.\n  Small table → more overcounting → less memory.\n\nYOUR FOOD APP:\n  Track 'top searched items' among millions of menu items.\n  Track 'how often each restaurant appears in search results'.\n  Track 'how many requests per API endpoint per minute'.\n  Fits in KB of memory instead of GB.\n\nTHE BIG PICTURE:\nWhen EXACT answers don't matter and APPROXIMATE is good enough, probabilistic data structures give you answers in:\n  - Constant memory regardless of data size\n  - Constant time regardless of data size\n  - Small, bounded error\n\nGoogle, Facebook, Twitter use these at massive scale. 'Top 10 trending topics' isn't computed exactly — it's estimated with Count-Min Sketch.",
    analogy:"🍕 HyperLogLog = counting unique customers without remembering every face. You note the most 'unusual' thing about each customer (longest streak of specific eye colors, rarest tattoo). From the rarest thing you saw, you estimate roughly how many customers visited. 2% error, but you only wrote down one number. Count-Min Sketch = instead of a separate ledger for each menu item's popularity (millions of ledgers), you have a tiny grid. Every order fills in a few cells. To check 'how popular is biryani?', look up the cells for biryani and take the smallest count."
  },
  // ============ ARCHITECTURE ============
  { id:5, section:"architecture", phase:"CLIENT-SERVER", title:"Client-Server, Monolith & Microservices", icon:"🏛️", color:"#4A90D9",
    concepts:["Client-Server","Monolith","Microservices","Evolution"],
    actors:["Client","Server","Single Codebase","Multiple Services"],
    simple:"The foundational question: how do you organize your code? Three classic approaches — and your app will likely go through all three as it grows.\n\n1. CLIENT-SERVER: the basic model. A client (browser, mobile app) requests, a server responds.\n\n2. MONOLITH: ONE big application that does everything. All features, all logic, ONE codebase, ONE deployment.\n\n3. MICROSERVICES: dozens of small services, each handling ONE thing (payments, orders, notifications). Each deployed independently.",
    detail:"━━━ CLIENT-SERVER ━━━\n\nThe fundamental web architecture:\n  Client (browser/app) sends request → Server processes → Response.\n  Examples: every website, every mobile app.\n\n  Client: renders UI, handles user interactions.\n  Server: business logic, data storage, authentication.\n\n  Almost every architecture is a variation of client-server.\n\n━━━ MONOLITH — one big app ━━━\n\nYour food delivery app, monolith version:\n  Single Node.js application containing:\n    - User authentication code\n    - Restaurant catalog code\n    - Order management code\n    - Payment processing code\n    - Notification sending code\n    - Admin dashboard code\n  Single database.\n  Single deploy unit.\n\n✅ ADVANTAGES:\n  - Simple to develop (everything in one place)\n  - Easy to test locally (run one process)\n  - Easy to deploy (deploy one artifact)\n  - No network calls between features (function calls instead)\n  - Perfect for small teams and early-stage startups\n\n❌ DISADVANTAGES (at scale):\n  - Any tiny change requires deploying the WHOLE app\n  - Teams of 100+ stepping on each other in same codebase\n  - Payment team wants Java, ML team wants Python → tough luck, it's all one language\n  - Scaling: must scale the ENTIRE app even if only payments are hot\n  - One buggy module can crash the entire app\n\nSTART HERE. Seriously. Most 'microservices disasters' are from startups who should have been monoliths.\n\n━━━ MICROSERVICES — many small apps ━━━\n\nYour food delivery app, microservices version:\n  - Auth Service (Java)\n  - Restaurant Service (Python)\n  - Order Service (Node.js)\n  - Payment Service (Go)\n  - Notification Service (Node.js)\n  - Driver Service (Java)\n  Each has its OWN database, deployment, team.\n\nServices communicate via APIs (REST, gRPC) or events (Kafka).\n\n✅ ADVANTAGES:\n  - Independent deployment (payment team deploys without waiting)\n  - Technology freedom (each service picks best tools)\n  - Independent scaling (just scale the Order Service, not everything)\n  - Fault isolation (Notification crashes? Orders still work)\n  - Teams own their services end-to-end\n\n❌ DISADVANTAGES:\n  - NETWORK! Function calls become network calls (1ns → 10ms)\n  - Distributed transactions are hard (remember SAGA pattern?)\n  - Debugging: trace a bug across 10 services? Nightmare\n  - Operational complexity: 50 services × monitoring × deploy × networking\n  - Data consistency: each service has its own DB, no cross-service JOINs\n\nNOT A BEGINNER CHOICE. Only transition when the monolith's pain outweighs the distributed complexity.\n\n━━━ THE REAL ANSWER — MODULAR MONOLITH ━━━\n\nBefore microservices, consider: a well-organized monolith.\n  - Clear modules with explicit boundaries\n  - Each module has its own schema in the shared database\n  - Internal APIs between modules\n  - Can extract a module to a microservice LATER if needed\n\nThis gets you 80% of microservices benefits with 10% of the pain. Shopify, Basecamp, GitHub all run on modular monoliths.\n\nYOUR FOOD APP EVOLUTION:\n  Year 1 (10K users): Monolith. One Node.js app, one PostgreSQL.\n  Year 2 (1M users): Modular monolith. Clear boundaries, still one deploy.\n  Year 3 (10M users, 50-person team): Extract the hottest service (Payment) to microservice.\n  Year 4 (100M users): Full microservices for domains that need independent scaling.",
    analogy:"🍕 Monolith = one giant restaurant with one kitchen doing everything. Simple to manage, but if the oven breaks, the whole restaurant is down. Adding a sushi menu means retraining the whole staff. Microservices = a food court with specialized stalls. Each stall has its own kitchen, staff, even cuisine. But customers must walk between stalls, and coordinating a full meal across stalls is complex. Start with one kitchen. Add stalls only when the kitchen can't keep up."
  },
  { id:6, section:"architecture", phase:"EVENT-DRIVEN", title:"Event-Driven, CQRS & Event Sourcing", icon:"📡", color:"#9B59B6",
    concepts:["Event-Driven","CQRS","Event Sourcing","Serverless"],
    actors:["Event Bus","Command Handler","Query Handler","Event Store"],
    simple:"Advanced architectural patterns for complex systems.\n\nEVENT-DRIVEN: services don't CALL each other. They publish EVENTS and react to events from others. Loose coupling, built on message queues.\n\nCQRS (Command Query Responsibility Segregation): use DIFFERENT models for reads and writes. One model optimized for writing, another optimized for reading.\n\nEVENT SOURCING: don't store current state — store every EVENT that led to the current state. Replay events to reconstruct state.\n\nSERVERLESS: no servers to manage. Write functions, cloud provider runs them on demand.",
    detail:"━━━ EVENT-DRIVEN ARCHITECTURE ━━━\n\nTRADITIONAL (request-response):\n  Order Service → calls Payment Service\n  Order Service → calls Inventory Service\n  Order Service → calls Notification Service\n  Order Service KNOWS about all these services.\n  Adding Recommendation Service? Modify Order Service code.\n\nEVENT-DRIVEN:\n  Order Service publishes event: 'OrderPlaced'\n  Payment Service subscribes to 'OrderPlaced' → charges\n  Inventory Service subscribes → reserves stock\n  Notification Service subscribes → sends SMS\n  Order Service DOESN'T KNOW who's listening.\n  Adding Recommendation Service? Just subscribe to 'OrderPlaced'. Zero changes to Order Service.\n\nINVERTED DEPENDENCIES:\n  Traditional: Order Service depends on Payment Service.\n  Event-driven: Payment Service depends on 'OrderPlaced' events (abstraction).\n\nBuilt on top of: Kafka, RabbitMQ, AWS EventBridge.\n\n━━━ CQRS — Command Query Responsibility Segregation ━━━\n\nOBSERVATION: Read patterns and write patterns are VERY different.\n\nWRITES: need validation, business rules, consistency.\n  'Place order' → check inventory, verify payment, apply discounts, update user loyalty points, etc.\n  Complex, normalized database (5+ tables).\n\nREADS: need speed, simple structure.\n  'Show my order history' → just display what happened.\n  Prefer denormalized, read-optimized data.\n\nCQRS: use DIFFERENT MODELS.\n\n  COMMAND side (writes):\n    → Normalized PostgreSQL with strict constraints\n    → Validates business rules\n    → Emits events\n\n  QUERY side (reads):\n    → Denormalized views (e.g., MongoDB)\n    → Pre-computed for fast display\n    → Updated by consuming events from command side\n\nYOUR FOOD APP:\n  Write: 'Place order' → PostgreSQL (Orders, OrderItems, Payments tables) → emit 'OrderPlaced' event\n  Read: 'Show my orders' → MongoDB 'OrderHistoryView' (all info denormalized, fast to display)\n  Event listener keeps MongoDB view updated from PostgreSQL changes.\n\n✅ Each side independently optimized\n❌ More infrastructure, eventual consistency between write and read models\n\nUSE WHEN: dramatic read/write imbalance, different scaling needs, complex business rules.\n\n━━━ EVENT SOURCING ━━━\n\nTRADITIONAL: store CURRENT STATE.\n  orders table:\n    id=456, status='DELIVERED', total=640\n  You only know the current state. How did it get here? Lost.\n\nEVENT SOURCING: store EVERY EVENT that changed state.\n  events table:\n    t=12:00: OrderCreated {id:456, total:640}\n    t=12:01: PaymentCharged {order:456, amount:640}\n    t=12:05: OrderAccepted {order:456, restaurant:Meghana}\n    t=12:15: DriverAssigned {order:456, driver:Amit}\n    t=12:45: OrderDelivered {order:456, time:12:45}\n\nCURRENT STATE is computed by REPLAYING events:\n  Start with empty state → apply OrderCreated → apply PaymentCharged → etc.\n  After all events: status='DELIVERED', total=640.\n\n✅ COMPLETE AUDIT LOG (who did what when, automatically)\n✅ TIME TRAVEL ('what was the order state at 12:30?' → replay events up to 12:30)\n✅ FIX BUGS RETROACTIVELY ('we computed discounts wrong — replay with fixed logic')\n✅ Perfect for regulated industries (banking, healthcare)\n\n❌ Complex to implement\n❌ Events are IMMUTABLE and FOREVER (growing storage)\n❌ Schema changes are hard (old events may not match new code)\n❌ Queries are slower (must replay)\n\nOften combined with CQRS: event source the write side, project to multiple read models.\n\n━━━ SERVERLESS ━━━\n\nNO SERVERS TO MANAGE. You write functions. Cloud runs them when triggered.\n\nAWS Lambda example:\n  function handleOrder(event) {\n    // your code\n    return result;\n  }\n  \n  Triggered by: HTTP request, Kafka message, S3 upload, cron schedule.\n  Runs for milliseconds to minutes. Pay only for execution time.\n  Auto-scales from 0 to 10,000 invocations/sec.\n\n✅ No infrastructure management (no servers, no OS patching)\n✅ Pay-per-use (nothing running = no cost)\n✅ Automatic scaling\n\n❌ COLD STARTS (first request to a cold function takes 1-3 seconds)\n❌ Stateless (can't hold connections, must re-setup each call)\n❌ Vendor lock-in (AWS Lambda code doesn't run on Google)\n❌ Not cost-effective for constant high traffic\n\nUSE FOR: sporadic workloads, event processors, scheduled jobs, low-traffic APIs.\n\nYOUR FOOD APP:\n  Image resize on upload → S3 trigger → Lambda → resize & save\n  Weekly analytics report → CloudWatch cron → Lambda\n  Webhook receiver (Razorpay) → API Gateway → Lambda\n  Not for: main order API (constant traffic, cold starts unacceptable).\n\n━━━ P2P (Peer-to-Peer) ━━━\n\nNo central server. Nodes communicate directly with each other.\nExamples: BitTorrent (file sharing), blockchain (Bitcoin), old Napster/Kazaa.\nUsed rarely in business apps — centralized servers are simpler to control and secure.\nRelevant for: decentralized applications, censorship-resistant systems, crypto.",
    analogy:"🍕 Event-driven = when a big order arrives, don't individually tell the kitchen, the waiter, the bartender. Announce 'Order #456 placed!' over the intercom. Each station listens and does its part. Adding a new station (dessert chef) = they just start listening. CQRS = separate kitchens for taking orders (complex) vs serving food (fast, pre-plated). Event sourcing = instead of just tracking 'bill total', keep a running log of every order, discount, and modification. Serverless = instead of permanent kitchens, just hire chefs per order — pay only when cooking happens."
  },
  // ============ MICROSERVICES PATTERNS ============
  { id:7, section:"microservices", phase:"DISCOVERY", title:"Service Discovery, API Gateway & BFF", icon:"🔎", color:"#50C878",
    concepts:["Service Discovery","API Gateway","BFF","Service Registry"],
    actors:["Service Registry","Services","API Gateway","Client Apps"],
    simple:"Once you have 50 microservices, practical problems emerge. How does the Order Service find the Payment Service's address? That's SERVICE DISCOVERY. How do mobile apps avoid calling 50 different services? That's the API GATEWAY. Different clients (web, mobile, smart TV) need different data shapes. That's BFF (Backend For Frontend).",
    detail:"━━━ SERVICE DISCOVERY ━━━\n\nPROBLEM: Payment Service runs on multiple instances (auto-scaling).\n  Instance 1: 10.0.1.15:8080\n  Instance 2: 10.0.1.16:8080\n  Instance 3: 10.0.1.17:8080 (just started)\n  Instance 4: 10.0.1.18:8080 (just died)\n\nOrder Service needs to call Payment Service. Which IP? Hardcoding is impossible because instances come and go.\n\nSOLUTION: SERVICE REGISTRY\n  A central database of 'which services exist, where are they, are they healthy?'\n\nHOW IT WORKS:\n  1. When Payment Service instance starts:\n     → Register with registry: 'I'm payment-service at 10.0.1.15:8080'\n     → Send heartbeats every 5s to stay registered\n  \n  2. When Payment Service instance dies:\n     → No heartbeats → registry removes it after timeout\n  \n  3. When Order Service wants to call Payment:\n     → Ask registry: 'Where is payment-service?'\n     → Registry returns: [10.0.1.15:8080, 10.0.1.16:8080, 10.0.1.17:8080]\n     → Order Service picks one (load balancing) and calls it\n\nTOOLS:\n  - Consul (HashiCorp)\n  - etcd (CoreOS / Kubernetes)\n  - Eureka (Netflix)\n  - Kubernetes Service (built-in)\n\nTWO PATTERNS:\n  CLIENT-SIDE DISCOVERY: client asks registry directly, picks an instance.\n  SERVER-SIDE DISCOVERY: client calls a load balancer, which asks registry.\n\n━━━ API GATEWAY ━━━\n\nPROBLEM: Rahul's mobile app needs data from 8 different services per screen.\n  Making 8 separate network calls from mobile = slow, battery-draining, complex.\n\nSOLUTION: API GATEWAY — single entry point for all clients.\n\n  Mobile app → API Gateway → routes to Order/Payment/Restaurant/... services\n\nRESPONSIBILITIES:\n  - ROUTING: /orders/* → Order Service, /payments/* → Payment Service\n  - AUTHENTICATION: validate JWT once, don't make every service validate\n  - RATE LIMITING: centralized throttling (1000 req/user/min)\n  - AGGREGATION: combine multiple service calls into one response\n  - PROTOCOL TRANSLATION: mobile speaks REST, internal services speak gRPC\n  - CACHING: cache common responses at the gateway\n  - LOGGING: single place to log all API traffic\n\nTOOLS: Kong, AWS API Gateway, Nginx, Envoy, Apigee.\n\n━━━ BFF (Backend For Frontend) ━━━\n\nPROBLEM: Different clients need different data.\n  Mobile app: minimal data, small payloads (slow networks)\n  Web app: rich data, full details\n  Smart TV app: video-focused data\n  Admin dashboard: entire-system view\n\nONE API serving all of them → compromise that satisfies none.\n\nSOLUTION: ONE BACKEND PER FRONTEND.\n\n  Mobile BFF: tailored for mobile needs\n  Web BFF: tailored for web needs\n  TV BFF: tailored for TV needs\n  Each BFF aggregates from the same downstream microservices.\n\n  Mobile App → Mobile BFF → [Order Service, Restaurant Service, User Service]\n  Web App → Web BFF → [Same services, different queries/aggregations]\n\nEach BFF is a small API Gateway specifically for one client type.\n\n✅ Each client gets EXACTLY the data it needs\n✅ Mobile team owns mobile BFF, doesn't depend on web team\n✅ Can evolve independently\n\n❌ More services to maintain\n❌ Some code duplication across BFFs\n\nYOUR FOOD APP:\n  iOS app → iOS BFF → underlying services\n  Android app → Android BFF → underlying services\n  Restaurant dashboard → Restaurant BFF → underlying services\n  Admin panel → Admin BFF → underlying services\n  \n  Each BFF team controls their own API without affecting others.",
    analogy:"🍕 Service discovery = a directory of which branches are open right now, their phone numbers, and whether they're too busy to take orders. API Gateway = one central customer service hotline that routes your call to the right branch (billing, orders, complaints). BFF = different customer service numbers for app users, website users, corporate clients — each tailored to that audience."
  },
  { id:8, section:"microservices", phase:"RESILIENCE", title:"Sidecar, Circuit Breaker, Bulkhead & Strangler", icon:"🛡️", color:"#F39C12",
    concepts:["Sidecar","Circuit Breaker","Bulkhead","Strangler Fig","Service Mesh"],
    actors:["Main Service","Sidecar Container","Failed Service","Isolated Pools"],
    simple:"When services depend on each other, failure modes are tricky. These patterns keep your system resilient.\n\nSIDECAR: a helper container deployed alongside your service that handles cross-cutting concerns (logging, mTLS, retries). Your service focuses on business logic.\n\nCIRCUIT BREAKER: stop calling a failing service to prevent cascade failures.\n\nBULKHEAD: isolate resources so one service's failure can't exhaust resources for others.\n\nSTRANGLER FIG: gradually replace a legacy monolith by moving features to new services one at a time.\n\nSERVICE MESH: infrastructure layer handling all service-to-service communication.",
    detail:"━━━ SIDECAR PATTERN ━━━\n\nPROBLEM: Every service needs the SAME cross-cutting concerns:\n  - Logging to ELK\n  - Metrics to Prometheus\n  - mTLS certificate management\n  - Retry logic with backoff\n  - Rate limiting\n  - Service discovery\n\nImplementing these 6 times in 6 services (Java, Python, Node.js...) = wasted effort.\n\nSIDECAR SOLUTION: Attach a helper container to each service pod.\n\n  ┌─────────────────────────┐\n  │  Kubernetes Pod         │\n  │  ┌───────┐  ┌────────┐  │\n  │  │ Main  │──│Sidecar │  │\n  │  │Service│  │(Envoy) │  │\n  │  └───────┘  └────────┘  │\n  └─────────────────────────┘\n\nMain service only makes local HTTP calls (to localhost:8080 → sidecar).\nSidecar handles everything: TLS, routing, retries, metrics.\nSidecar is implemented once, used by every service regardless of language.\n\nWHO USES IT: Envoy as sidecar in Istio service mesh. Istio handles:\n  - mTLS between services\n  - Traffic routing (canary deployments)\n  - Observability (distributed tracing automatically)\n  - Security policies\n\n━━━ CIRCUIT BREAKER ━━━\n\nPROBLEM: Payment Service is down. Order Service keeps calling it.\n  Each call waits 30 seconds before timing out.\n  Under load: Order Service threads pile up waiting for dead Payment Service.\n  Order Service itself becomes unresponsive. Cascading failure!\n\nCIRCUIT BREAKER SOLUTION: like an electrical breaker that TRIPS when overloaded.\n\nTHREE STATES:\n\n  CLOSED (normal): calls flow through. Track failure rate.\n    If failures > threshold (e.g., 50% of last 20 calls) → trip to OPEN\n\n  OPEN (blocked): reject all calls INSTANTLY (no waiting).\n    Returns cached response or error immediately.\n    After cooldown (30s), transition to HALF_OPEN.\n\n  HALF_OPEN (testing): allow ONE test call.\n    Success → back to CLOSED (service recovered)\n    Failure → back to OPEN (still down, wait more)\n\nBenefits:\n  - Fast failure (no waiting for dead services)\n  - Gives failing service time to recover\n  - Prevents cascade failures\n\nImplementation: Hystrix (Netflix, classic), Resilience4j (modern), Polly (.NET).\n\n━━━ BULKHEAD PATTERN ━━━\n\nNamed after ship design — bulkheads divide the hull so one leak doesn't sink the ship.\n\nPROBLEM: Your service has a thread pool of 200 threads.\n  Normal requests use 10 threads.\n  Payment Service becomes slow → requests to Payment use 190 threads (stuck waiting).\n  Other requests (health checks, other APIs) → only 10 threads → queue up → timeout.\n  Entire service fails because of ONE slow dependency.\n\nBULKHEAD SOLUTION: Separate thread pools per dependency.\n\n  Payment Service calls → dedicated pool of 50 threads\n  Restaurant Service calls → dedicated pool of 50 threads\n  Inventory Service calls → dedicated pool of 50 threads\n  Other calls → pool of 50 threads\n\nIf Payment Service is slow → only its 50-thread pool gets stuck.\nOther pools are isolated and continue working.\n\n━━━ STRANGLER FIG PATTERN ━━━\n\nNamed after the fig tree that grows around a host tree, eventually replacing it.\n\nPROBLEM: You have a giant legacy monolith. You want microservices. But:\n  - Can't stop and rewrite (business must continue)\n  - Can't do big-bang replacement (too risky)\n\nSOLUTION: Gradually strangle the monolith.\n\n  Step 1: Put an API Gateway / proxy in front of the monolith.\n          All traffic still goes to monolith.\n  \n  Step 2: Extract ONE feature (e.g., Notifications) into a new microservice.\n          Configure proxy: /notifications/* → new service.\n                         /* → monolith (everything else).\n  \n  Step 3: Notifications microservice is working. Extract another feature (Payments).\n          Proxy: /notifications/* → new, /payments/* → new, /* → monolith.\n  \n  Step 4: Repeat for each feature.\n  \n  Step 5: Eventually, monolith has nothing left. Retire it.\n\nTakes YEARS. But every step is safe. You can roll back individual features.\n\n━━━ SERVICE MESH ━━━\n\nThe modern evolution: make the network itself handle all these patterns.\n\nService Mesh = infrastructure layer handling service-to-service communication.\n\nExamples: Istio, Linkerd, Consul Connect.\n\nWHAT IT PROVIDES (for free, without code changes):\n  - mTLS between all services (automatic encryption)\n  - Retries with exponential backoff\n  - Circuit breaking\n  - Load balancing\n  - Distributed tracing\n  - Traffic splitting (canary deployments)\n  - Rate limiting\n  - Service discovery\n\nYour application code has NO idea the mesh exists. It just makes regular HTTP calls.\nThe sidecar proxy (Envoy) intercepts and handles everything.\n\nUSED BY: Uber, Airbnb, Lyft at scale. Overkill for small projects.\n\nYOUR FOOD APP PROGRESSION:\n  10 services: manual retry logic, manual TLS, basic monitoring\n  50 services: adopt Istio → automatic mTLS, tracing, retries for free\n  Your code becomes simpler. Infrastructure handles the complexity.",
    analogy:"🍕 Sidecar = every kitchen has a dedicated helper who handles orders, billing, and coordination — chef focuses on cooking. Circuit breaker = if the pizza oven fails repeatedly, stop sending pizza orders to it. Wait 30 seconds, try one test order, if ok resume, if not wait again. Bulkhead = separate kitchens for pizza vs sushi vs indian. If one kitchen catches fire, others keep running. Strangler fig = slowly renovating a restaurant one section at a time instead of closing for a year. Service mesh = installing an automated traffic control system across all your 50 restaurants so managers don't have to manually coordinate deliveries."
  },
  // ============ BIG DATA ============
  { id:9, section:"bigdata", phase:"BATCH VS STREAM", title:"Batch vs Stream Processing", icon:"⚡", color:"#E74C3C",
    concepts:["Batch Processing","Stream Processing","MapReduce","ETL"],
    actors:["Data Source","Batch Jobs","Stream Processors","Analytics Store"],
    simple:"Your food app generates massive data: 10 million orders/day, 100 million location pings/day, 1 billion events/day. You need to analyze this to make business decisions. But HOW?\n\nBATCH PROCESSING: collect data over a period (hour, day), then process it all at once. Great for deep analysis, not for real-time needs.\n\nSTREAM PROCESSING: process data as it arrives, continuously. Perfect for real-time dashboards, fraud detection, live analytics.\n\nMost systems use BOTH. Lambda and Kappa architectures describe how.",
    detail:"━━━ BATCH PROCESSING ━━━\n\nCOLLECT then PROCESS.\n\nExample — Daily revenue report:\n  Every night at 2 AM:\n    1. Read all orders from today (10 million rows)\n    2. Group by restaurant\n    3. Sum revenue per restaurant\n    4. Write results to analytics database\n    5. Dashboard shows yesterday's data\n\nCHARACTERISTICS:\n  - Processes LARGE volumes at once\n  - LATENCY: hours to days (you see yesterday's data)\n  - THROUGHPUT: very high\n  - SIMPLER to write and debug (static input)\n\nTOOLS: Apache Hadoop, Apache Spark, AWS EMR, Google Dataflow (batch mode).\n\n━━━ MapReduce — the granddaddy of batch processing ━━━\n\nGOOGLE'S 2004 PAPER that changed everything.\n\nPROBLEM: Count word frequencies across 1 TB of text. One machine can't do it.\n\nMAP PHASE (parallel across 100 machines):\n  Each machine gets a chunk of the text.\n  For each word, emit (word, 1).\n  Example input: 'the quick brown fox'\n  Output: (the, 1), (quick, 1), (brown, 1), (fox, 1)\n\nSHUFFLE PHASE:\n  System groups all pairs with the same key.\n  (the, 1), (the, 1), (the, 1) → (the, [1,1,1])\n\nREDUCE PHASE (parallel):\n  For each key, sum the values.\n  (the, [1,1,1]) → (the, 3)\n\nFinal output: (the, 1500000), (quick, 234), (brown, 567), ...\n\nKey insight: ANY parallelizable computation can fit into Map + Reduce.\nThis gave us distributed processing for anyone, not just Google.\n\nModern alternatives (Spark) are more flexible but built on the same idea.\n\n━━━ STREAM PROCESSING ━━━\n\nEACH EVENT processed as it arrives.\n\nExample — Real-time fraud detection:\n  Customer swipes card.\n  Transaction flows through stream processor in milliseconds.\n  Check: too many swipes in last minute? Geographic impossibility?\n  Decision: allow or block.\n  Happens before the payment completes.\n\nCHARACTERISTICS:\n  - Processes ONE event at a time (or small windows)\n  - LATENCY: milliseconds to seconds\n  - Can't access 'all the data' — only see what's happened so far\n  - More complex (time windows, late-arriving data, state management)\n\nTOOLS: Apache Kafka Streams, Apache Flink, Apache Storm, AWS Kinesis.\n\nYOUR FOOD APP — batch vs stream examples:\n\nBATCH USE CASES:\n  - Monthly revenue reports\n  - ML model training (feed millions of orders, learn patterns)\n  - Data warehouse nightly aggregations\n  - Historical analysis ('which restaurants grew this year?')\n\nSTREAM USE CASES:\n  - Live dashboard ('orders per minute right now')\n  - Fraud detection during payment\n  - Dynamic pricing (surge pricing during high demand)\n  - Real-time recommendations ('Rahul just ordered biryani, suggest raita')\n  - Live order tracking (driver location updates)\n\n━━━ ETL PIPELINES ━━━\n\nExtract, Transform, Load.\n\nEXTRACT: pull data from sources (OLTP database, logs, APIs)\nTRANSFORM: clean, filter, aggregate, join\nLOAD: write to destination (data warehouse)\n\nYOUR FOOD APP ETL:\n  Extract: orders from PostgreSQL, events from Kafka, logs from S3\n  Transform: deduplicate, enrich with restaurant info, aggregate by hour/region\n  Load: write to data warehouse (Snowflake, BigQuery)\n\nELT (variant): Extract, Load, then Transform inside the warehouse.\n  Modern cloud warehouses are powerful enough to transform in-place.\n\nTOOLS: Apache Airflow (orchestrator), dbt (transformations), Fivetran (extract).",
    analogy:"🍕 Batch = accountant reviewing all receipts at month-end and producing a report. Thorough but 30 days late. Stream = bouncer watching every customer entering and deciding immediately who's underage. Real-time but can't see the big picture. MapReduce = 100 accountants each given a stack of receipts. They each tally their stack (map), then group receipts by category (shuffle), then teams sum each category (reduce). 1 day of work becomes 1 hour."
  },
  { id:10, section:"bigdata", phase:"STORAGE", title:"Data Lakes, Warehouses & Lakehouses", icon:"🏞️", color:"#1ABC9C",
    concepts:["Data Lake","Data Warehouse","Lakehouse","Lambda","Kappa"],
    actors:["Raw Data","Data Lake (S3)","Data Warehouse","Lakehouse"],
    simple:"Where do you STORE all the data for analytics? Not in your production PostgreSQL — that's for live operations. You need analytical storage. Three main approaches:\n\nDATA LAKE: dump all raw data (structured, unstructured) into cheap storage like S3. Process later when needed.\n\nDATA WAREHOUSE: structured, cleaned, optimized for analytical queries. Expensive but fast.\n\nDATA LAKEHOUSE: the modern hybrid — lake-like storage with warehouse-like query performance.",
    detail:"━━━ DATA LAKE ━━━\n\nDumping ground for ALL data, in any format.\n  - Structured: tables from databases\n  - Semi-structured: JSON logs, XML\n  - Unstructured: images, videos, text\n\nTYPICAL TECH: Amazon S3, Azure Data Lake, Google Cloud Storage.\nFile formats: Parquet, ORC, Avro (for analytics), plus raw logs, images, etc.\n\nSCHEMA-ON-READ: store data without defining schema. When querying, interpret structure.\n  Flexible, but queries can be slow and error-prone.\n\nPROS:\n  ✅ Cheap storage (~$0.023/GB/month on S3)\n  ✅ Stores everything, even if you don't know how you'll use it\n  ✅ Scales to petabytes easily\n\nCONS:\n  ❌ 'Data swamp' if not organized (random data, no one knows what's there)\n  ❌ Slow for direct querying (no indexes, no optimization)\n  ❌ Needs separate tools to process (Spark, Presto, Athena)\n\nYOUR FOOD APP DATA LAKE:\n  s3://foodapp-data-lake/\n    raw/orders/2024/01/15/orders.parquet\n    raw/events/kafka-logs/*.json\n    raw/images/restaurant-photos/*\n    raw/logs/app-logs/*\n  \n  All historical data, cheap to store forever.\n\n━━━ DATA WAREHOUSE ━━━\n\nOptimized for ANALYTICAL QUERIES on structured data.\n\nKEY FEATURES:\n  - COLUMNAR storage (stores columns together, not rows — perfect for aggregations)\n  - Massive parallelism (queries run on many nodes)\n  - SQL interface\n  - Pre-built indexes and statistics\n\nTYPICAL TECH: Snowflake, BigQuery, Amazon Redshift, Azure Synapse.\n\nSCHEMA-ON-WRITE: data must conform to schema before loading. Clean, structured.\n\nPROS:\n  ✅ FAST queries (designed for analytics)\n  ✅ SQL — familiar to analysts\n  ✅ Mature ecosystem (BI tools integrate natively)\n\nCONS:\n  ❌ Expensive (~10x cost of data lake)\n  ❌ Requires ETL/ELT pipeline\n  ❌ Schema rigid — hard to change\n\nYOUR FOOD APP WAREHOUSE:\n  Snowflake with dimensional schema:\n    fact_orders (order_id, user_id, restaurant_id, total, date_id, ...)\n    dim_users (user_id, name, city, join_date, ...)\n    dim_restaurants (restaurant_id, name, cuisine, city, ...)\n    dim_dates (date_id, day, month, quarter, year, weekday)\n  \n  Analysts run: 'Total revenue by restaurant by month' → answers in seconds.\n\n━━━ DATA LAKEHOUSE — the modern hybrid ━━━\n\nLakes are cheap but slow. Warehouses are fast but expensive.\nWhy not have both? LAKEHOUSE = data lake + warehouse query performance.\n\nKEY IDEA: use open table formats (Apache Iceberg, Delta Lake, Apache Hudi) on top of object storage (S3) to get ACID transactions, schema evolution, time travel, AND warehouse-speed queries.\n\nTYPICAL TECH: Databricks (Delta Lake), AWS (Iceberg + Athena), Dremio.\n\nPROS:\n  ✅ Cheap storage (just S3)\n  ✅ Fast queries (thanks to open formats and query engines)\n  ✅ Flexible (handles structured and unstructured)\n  ✅ Single source of truth (no duplicate copies in lake + warehouse)\n\nWHY EVERYONE'S MOVING TO LAKEHOUSES: 80% of the warehouse performance at 20% of the cost.\n\n━━━ LAMBDA ARCHITECTURE ━━━\n\nTraditional big data architecture combining batch + stream.\n\nTwo pipelines running in parallel:\n\n  BATCH LAYER (speed: hours, accuracy: perfect):\n    All data → Hadoop/Spark → daily aggregates → batch views\n  \n  SPEED LAYER (speed: seconds, accuracy: approximate):\n    Real-time events → Kafka/Flink → streaming aggregates → speed views\n  \n  SERVING LAYER:\n    Merge batch + speed views to answer queries.\n    Give users the best of both: real-time approximation + eventually perfect batch.\n\nPROBLEM: maintaining two codebases (one for batch, one for stream) is a NIGHTMARE.\nYou write business logic twice. Debugging inconsistencies between them is painful.\n\n━━━ KAPPA ARCHITECTURE ━━━\n\nSimpler alternative: ONLY stream processing.\n\n  All data flows through Kafka (forever).\n  Stream processors consume events and build views.\n  Need to reprocess? Replay from Kafka from the beginning.\n\nOne codebase, one system. Kafka retains data long enough (weeks/months) for reprocessing.\n\nGAINING POPULARITY: storage is cheap, stream engines are powerful. Why have two pipelines?\n\nYOUR FOOD APP:\n  Ingest: orders, events → Kafka\n  Stream processing: Flink computes real-time metrics → Cassandra\n  For deeper historical analysis: dump Kafka → S3 → query with Spark/Athena\n  Single source of truth: Kafka. Everything else is a derived view.",
    analogy:"🍕 Data lake = your restaurant's storage warehouse. Receipts, video footage, old menus, customer feedback forms — all stored cheaply. Messy but everything's there. Data warehouse = a clean, organized accounting office. Every transaction properly categorized, instantly queryable, but expensive to maintain. Lakehouse = the warehouse has its storage IN the cheap warehouse, but smart indexing makes queries fast. Lambda = two teams, one doing yearly reports, one watching real-time. Kappa = one team handling everything through a single continuous pipeline."
  },
  // ============ OBSERVABILITY ============
  { id:11, section:"observability", phase:"OBSERVABILITY", title:"Logs, Metrics & Distributed Tracing", icon:"🔭", color:"#9B59B6",
    concepts:["Logs","Metrics","Traces","Three Pillars","Correlation ID"],
    actors:["Production Services","Log Aggregator","Metrics System","Trace Collector"],
    simple:"At 3 AM, your food app is slow. Users are complaining. WHICH of your 50 services is the problem? Without OBSERVABILITY, you're blind. The THREE PILLARS give you sight:\n\nLOGS: what happened (text records of events).\nMETRICS: how much/how often (numbers over time).\nTRACES: how a single request flowed through your system.\n\nTogether, they let you diagnose problems in minutes instead of hours.",
    detail:"━━━ LOGS — WHAT HAPPENED ━━━\n\nText records of events in your services.\n\nBASIC LOG:\n  [2024-01-15 12:30:45] INFO: User rahul_1 placed order 456\n  [2024-01-15 12:30:46] ERROR: Payment Service returned 500\n  [2024-01-15 12:30:47] WARN: Retry attempt 1 for payment\n\nSTRUCTURED LOGS (modern best practice):\n  {\n    \"timestamp\": \"2024-01-15T12:30:45.123Z\",\n    \"level\": \"INFO\",\n    \"service\": \"order-service\",\n    \"message\": \"Order placed\",\n    \"user_id\": \"rahul_1\",\n    \"order_id\": \"456\",\n    \"total\": 640,\n    \"trace_id\": \"abc123xyz\"\n  }\n\nWHY STRUCTURED: you can QUERY it. 'All errors from order-service in last hour' is a filter, not a grep.\n\nLOGGING BEST PRACTICES:\n  - Use levels consistently: DEBUG, INFO, WARN, ERROR, FATAL\n  - Don't log every successful request (too noisy)\n  - DO log: errors, important business events, unusual conditions\n  - Include context: user_id, order_id, trace_id\n  - NEVER log sensitive data (passwords, full credit card numbers, tokens)\n  - Use async logging (don't slow down requests)\n\nLOG AGGREGATION:\nYour 50 services run on 200 servers. Logs are scattered. You need central collection.\n\nPIPELINE:\n  Service writes logs → log collector (Fluentd, Filebeat) → message queue (Kafka) → \n  storage + indexing (Elasticsearch) → visualization (Kibana)\n\nThis is the ELK stack (Elasticsearch, Logstash, Kibana) or similar.\n\nNOW you can search: 'error AND service:payment AND last 30 minutes' → instantly see all payment errors.\n\n━━━ METRICS — HOW MUCH ━━━\n\nNumbers tracked over time.\n\nEXAMPLES:\n  orders_placed_per_minute: 342 (counter)\n  response_time_p99_ms: 234 (gauge/histogram)\n  database_connections_active: 47 of 100 (gauge)\n  error_rate_percentage: 0.3 (gauge)\n\nMETRIC TYPES:\n  COUNTER: only goes up (total requests, total orders)\n  GAUGE: goes up and down (current memory, active users)\n  HISTOGRAM: distribution of values (response time percentiles)\n\nTHE GOLDEN SIGNALS (what to monitor):\n  1. LATENCY: how long do requests take?\n  2. TRAFFIC: how many requests per second?\n  3. ERRORS: what percentage fail?\n  4. SATURATION: how full is the system (CPU, memory, queue depth)?\n\nTYPICAL STACK: Prometheus (collects metrics) + Grafana (visualizes).\n\nYOUR FOOD APP METRICS:\n  - orders_placed_total (counter)\n  - order_placement_latency_seconds (histogram) — p50, p95, p99\n  - payment_failures_total (counter)\n  - active_users_gauge (gauge)\n  - database_query_duration_seconds (histogram)\n  - kafka_consumer_lag (gauge)\n\nDASHBOARDS: Grafana panels showing these metrics visually.\n\n━━━ TRACES — FOLLOWING ONE REQUEST ━━━\n\nA single user request touches many services. How long did each service take?\n\nDISTRIBUTED TRACING:\n  Every request gets a unique TRACE ID (e.g., abc123xyz).\n  As the request flows through services, each service logs a 'span' with that trace ID.\n\nEXAMPLE TRACE for Rahul's order:\n\n  Trace ID: abc123xyz\n  \n  Span 1: API Gateway (5ms)\n    Span 2: Order Service (45ms)\n      Span 3: Database INSERT (20ms)\n      Span 4: call Payment Service (15ms)\n        Span 5: call Razorpay API (12ms)\n      Span 6: call Inventory Service (8ms)\n        Span 7: Database UPDATE (5ms)\n      Span 8: publish to Kafka (2ms)\n\nVISUALIZED as a waterfall chart, you see EXACTLY which step was slow.\n\nIF ORDER IS SLOW: trace shows which span took 500ms. Now you know where to optimize.\n\nTOOLS: Jaeger, Zipkin, AWS X-Ray, DataDog APM, OpenTelemetry (standard).\n\n━━━ CORRELATION IDS ━━━\n\nThe glue connecting logs, metrics, and traces.\n\nWhen a request enters your system, generate a correlation_id (UUID).\nPass it through EVERY service call (HTTP header: X-Correlation-ID).\nInclude it in EVERY log message.\n\nNow: user complains order 456 failed.\n  Search logs by correlation_id → see every log line for that request across all services.\n  Pull the trace → see the waterfall.\n  Check metrics at that time → any spike in errors?\n\nDEBUG TIME: minutes instead of hours.\n\n━━━ ALERTING ━━━\n\nDon't stare at dashboards 24/7. Let the system tell you when something's wrong.\n\nALERT RULES:\n  - error_rate > 5% for 5 minutes → page on-call\n  - p99_latency > 1 second for 10 minutes → page\n  - database_connections > 90% → warning\n  - payment_failures > 10/min → critical\n\nTOOLS: PagerDuty (routes alerts), Opsgenie, Slack integrations.\n\nALERT FATIGUE: if you alert on everything, nothing matters. Alert on REAL problems only.\n  Good alerts: things that require human action NOW.\n  Bad alerts: 'CPU is at 70%' when that's normal.\n\n━━━ DASHBOARDS & RUNBOOKS ━━━\n\nDASHBOARDS: visual real-time view of system health.\n  - Homepage dashboard: key metrics at a glance\n  - Service dashboards: per-service deep dive\n  - Business dashboards: revenue, orders, users\n\nRUNBOOKS: playbooks for common incidents.\n  'If payment error rate > 5%:\n    1. Check Razorpay status page\n    2. Check our Payment Service logs\n    3. Check database connections\n    4. If above doesn't help, contact Razorpay support\n    5. Post in #incidents channel'\n  \n  Junior engineers can handle incidents without senior help.\n\nYOUR FOOD APP OBSERVABILITY STACK:\n  Logs: Fluentd → Kafka → Elasticsearch → Kibana\n  Metrics: Prometheus → Grafana\n  Traces: OpenTelemetry → Jaeger\n  Alerts: PagerDuty\n  Correlation IDs: UUID in every request, every log line, every span.\n\nThis is your superpower for running a distributed system in production.",
    analogy:"🍕 Logs = security camera footage (detailed but overwhelming, you review when something specific happened). Metrics = health monitors on every kitchen (heart rate, temperature — know instantly if something's wrong). Traces = following one specific customer's order from the moment they walked in, to each station that handled it, to delivery. Correlation ID = wristband # assigned at the door — every interaction is tagged with it, you can reconstruct their entire experience. Alerts = sensors that notify you WHEN something's wrong, not constantly. Runbook = 'if the fire alarm goes off, do these 5 steps.'"
  },
  // ============ SECURITY ============
  { id:12, section:"security", phase:"SECURITY", title:"TLS, RBAC & Secrets Management", icon:"🔐", color:"#F39C12",
    concepts:["TLS","RBAC","Secrets Management","SAML"],
    actors:["Client","TLS Handshake","Permission System","Secrets Vault"],
    simple:"Four critical security topics for production systems.\n\nSSL/TLS: how your HTTPS connections are actually secured (deeper than the basic 'it's encrypted').\n\nRBAC (Role-Based Access Control): how to manage 'who can do what' as your org grows.\n\nSECRETS MANAGEMENT: where to store API keys, database passwords, certificates. NOT in your code!\n\nSAML: the enterprise SSO protocol — how corporate apps authenticate via Okta, Azure AD.",
    detail:"━━━ SSL/TLS DEEP DIVE ━━━\n\nYou learned the TLS handshake in the networking chapter. Let's go deeper.\n\nWHAT TLS ACTUALLY PROVIDES:\n  1. CONFIDENTIALITY: data is encrypted in transit (nobody can read it).\n  2. INTEGRITY: data can't be tampered with (if changed, it's detected).\n  3. AUTHENTICITY: you're really talking to who you think (server certificates).\n\nTHE HANDSHAKE (detailed):\n  1. Client sends 'Client Hello':\n     - Supported TLS versions (1.2, 1.3)\n     - Supported cipher suites (encryption algorithms)\n     - Random number (client_random)\n  \n  2. Server responds 'Server Hello':\n     - Chosen TLS version\n     - Chosen cipher suite\n     - Random number (server_random)\n     - Server's CERTIFICATE (contains public key, signed by Certificate Authority)\n  \n  3. Client verifies the certificate:\n     - Is it signed by a trusted CA?\n     - Is it expired?\n     - Does the domain match?\n  \n  4. KEY EXCHANGE (using Diffie-Hellman):\n     - Client and server each generate a temporary key pair.\n     - They exchange public keys.\n     - Each combines their private key with other's public key to derive the SAME SHARED SECRET.\n     - Nobody watching can derive this secret (even if they capture the entire exchange).\n  \n  5. Both sides use the shared secret to encrypt all further communication.\n\nCERTIFICATE AUTHORITIES (CAs):\n  Your browser comes with a list of ~150 trusted CAs (DigiCert, Let's Encrypt, etc.).\n  CAs sign website certificates to say 'yes, this certificate really belongs to foodapp.com'.\n  Without this trust chain, MITM attacks would be trivial.\n\nLET'S ENCRYPT: free CA that made HTTPS universal. Use certbot to auto-provision certs.\n\nMUTUAL TLS (mTLS):\n  Normal TLS: server proves identity to client.\n  mTLS: BOTH prove identity to each other.\n  \n  Use for: service-to-service communication in a microservices architecture.\n  Service A only trusts calls from services with valid certificates.\n  Istio service mesh provides mTLS automatically.\n\n━━━ RBAC — Role-Based Access Control ━━━\n\nPROBLEM: Your food app has 100 employees. How do you manage who can do what?\n\nBAD APPROACH: assign permissions individually.\n  Priya can: read_orders, update_restaurants, delete_users...\n  Amit can: read_orders, read_restaurants...\n  Unmaintainable. Priya changes role → forget to update permissions → security hole.\n\nRBAC: define ROLES with permissions, assign users to roles.\n\n  ROLES:\n    - customer_support: [read_orders, refund_order, view_user_profile]\n    - restaurant_owner: [read_own_restaurant, update_own_menu, read_own_orders]\n    - admin: [read_all, update_all, delete_all]\n    - developer: [read_logs, deploy_services]\n  \n  USERS → ROLES:\n    Priya → customer_support\n    Amit → developer + customer_support (multiple roles!)\n    Meghana → restaurant_owner\n    Rahul → admin\n\nCHECKING PERMISSIONS:\n  Before any action: 'Does this user's roles include the required permission?'\n  \n  if (!user.hasPermission('refund_order')) {\n    return 403 Forbidden;\n  }\n\nROLES ARE LARGER UNITS:\n  Priya gets promoted to Senior Support → add 'escalated_support' role.\n  She automatically gets all the extra permissions. No individual updates.\n\nABAC (Attribute-Based, more advanced):\n  Rules based on attributes:\n  'Allow if user.role='owner' AND resource.restaurant_id = user.restaurant_id'\n  More flexible, more complex.\n\nYOUR FOOD APP RBAC:\n  Check permission on every sensitive API call.\n  Roles stored in JWT claims.\n  Admin UI for managing roles.\n\n━━━ SECRETS MANAGEMENT ━━━\n\nPROBLEM: Your code needs:\n  - Database password: 'supersecretpassword123'\n  - Razorpay API key: 'rzp_live_abc123xyz'\n  - JWT signing secret: 'my_jwt_secret'\n  - AWS credentials\n\nWHERE DO YOU STORE THESE?\n\nBAD OPTIONS:\n  ❌ In code: committed to Git → leaked on GitHub → Razorpay key used by hackers\n  ❌ In environment variables in plain text files: still leakable, hard to rotate\n  ❌ In a shared Slack/Notion doc: no audit trail, no access control\n\nGOOD OPTION: DEDICATED SECRETS MANAGER.\n\nTOOLS:\n  - HashiCorp Vault (self-hosted, feature-rich)\n  - AWS Secrets Manager\n  - Azure Key Vault\n  - Google Secret Manager\n\nHOW IT WORKS:\n  1. Secrets stored encrypted in the vault.\n  2. Services authenticate to vault (via IAM role, machine identity).\n  3. At runtime, services fetch secrets from vault.\n  4. Secrets automatically rotate (database passwords change monthly).\n  5. Complete audit log: who accessed which secret when.\n\nBEST PRACTICES:\n  - NEVER commit secrets to code (use .gitignore, pre-commit hooks)\n  - Use machine identities (IAM roles) instead of static credentials\n  - Rotate secrets regularly (30-90 days)\n  - Use different secrets per environment (dev, staging, prod)\n  - Short-lived tokens instead of long-lived keys where possible\n\nEMERGENCY: secret leaked? Rotate IMMEDIATELY. Audit access logs.\n\n━━━ SAML — Security Assertion Markup Language ━━━\n\nEnterprise SSO protocol (from our earlier chapter).\n\nUSE CASE: Your food app serves restaurant OWNERS too. Big restaurant chains have their own Okta. They want their employees to log in to YOUR app using their corporate credentials.\n\nSAML FLOW (simplified):\n  1. Restaurant chain employee goes to foodapp.com/login\n  2. Your app redirects them to their Okta: 'authenticate this user'\n  3. Okta shows corporate login, employee enters creds\n  4. Okta sends signed SAML Assertion back to your app\n  5. Your app verifies the signature, creates a session\n\nIT'S VERBOSE:\n  <saml:Assertion>\n    <saml:Issuer>https://corp.okta.com</saml:Issuer>\n    <saml:Subject>employee@corp.com</saml:Subject>\n    <saml:Attribute Name='role'>restaurant_manager</saml:Attribute>\n    <ds:Signature>...XML sig...</ds:Signature>\n  </saml:Assertion>\n\nMODERN ALTERNATIVE: OIDC (from OAuth) — JSON-based, lighter, mobile-friendly.\n\nBUT SAML is still the enterprise standard for legacy apps. Your app must support both to serve enterprise customers.\n\nYOUR FOOD APP B2B:\n  For restaurant owners: OIDC with Google/Microsoft for small businesses\n  For big chains: SAML support for their existing corporate IdP\n  Both backed by your main auth service.\n\nEvery mature SaaS product supports both.",
    analogy:"🍕 TLS = sealed, tamper-proof courier bags with verified sender stamps. RBAC = role-based keycards — Manager card opens office+kitchen, Cook card opens only kitchen. Employees get cards matching their role, not individual keys. Secrets management = a central safe for all building keys, combo protected, with logs of who accessed what. Never keep keys in the receptionist's unlocked drawer (your code). SAML = corporate parking pass system where big companies' employees use their company ID to enter your parking lot instead of getting a separate pass."
  },
  // ============ BIG PICTURE ============
  { id:13, section:"security", phase:"FULL PICTURE", title:"The Complete Architecture — Everything Connected", icon:"🗺️", color:"#9B59B6",
    concepts:["Complete Architecture","All Patterns"],
    actors:["Rahul","CDN","API Gateway","Services","Data Platform","Observability"],
    simple:"Let's zoom all the way out. Your food delivery app's complete production architecture, now incorporating EVERYTHING we've covered across all 8 parts. This is what a real SDE-2 / system design interview answer looks like.",
    detail:"THE COMPLETE FOOD DELIVERY APP — PRODUCTION ARCHITECTURE:\n\n━━━ THE DATA STRUCTURES LAYER ━━━\n\n  Geohash on restaurants → proximity queries\n  Redis skip list → leaderboards (trending restaurants, top drivers)\n  R-Tree in PostGIS → complex geo queries\n  HyperLogLog → unique visitor counts (per day, per restaurant)\n  Count-Min Sketch → popular search terms\n  Merkle trees → data replication verification\n\n━━━ THE ARCHITECTURE ━━━\n\n  Modular monolith (early days) → microservices (at scale)\n  Event-driven via Kafka (loose coupling)\n  CQRS where reads/writes differ dramatically (order history)\n  Event sourcing for audit-critical flows (payment events)\n  Serverless (Lambda) for periodic jobs (nightly reports)\n\n━━━ THE MICROSERVICES ━━━\n\n  50+ services, deployed on Kubernetes.\n  Service Discovery via etcd/Kubernetes.\n  Service Mesh (Istio) provides: mTLS, retries, circuit breakers, tracing.\n  \n  API Gateway (Kong) → routes, auth, rate limits.\n  BFFs: Mobile BFF, Web BFF, Partner BFF (restaurant dashboards).\n  \n  Circuit Breakers on every external call.\n  Bulkheads isolating critical paths (payment) from less critical (notifications).\n\n━━━ THE DATA PLATFORM ━━━\n\nOPERATIONAL (OLTP):\n  PostgreSQL (sharded) — orders, users, payments\n  Redis Cluster — caching, sessions, rate limiting, geo\n  Elasticsearch — search, autocomplete\n  MongoDB — flexible menus, reviews\n\nANALYTICAL (OLAP):\n  Kafka → CDC from PostgreSQL → data lake (S3) + stream processor (Flink)\n  Flink → real-time dashboards + fraud detection\n  S3 data lake → Spark → data warehouse (Snowflake) → BI tools\n  Or: Lakehouse (Databricks) for unified approach\n  ClickHouse for fast analytics on hot data\n\nBATCH VIA AIRFLOW:\n  Nightly: ETL of PostgreSQL → data warehouse\n  Weekly: ML model training on historical data\n  Monthly: revenue reports, business analytics\n\nSTREAM VIA FLINK:\n  Real-time order volume dashboard\n  Fraud detection during payments\n  Dynamic pricing (surge during peak)\n  Live driver tracking\n\n━━━ THE RESILIENCE LAYER ━━━\n\nDISTRIBUTED SYSTEMS PATTERNS:\n  etcd (Raft consensus) for service registry, leader election\n  Heartbeats + Phi Accrual for failure detection\n  Gossip (Cassandra) for cluster membership\n  \nDISTRIBUTED TRANSACTIONS:\n  SAGA orchestration for order placement\n  Outbox pattern for reliable event publishing\n  Idempotent consumers for exactly-once semantics\n\nCONSISTENCY MODEL:\n  Strong consistency: payments (CP)\n  Eventual consistency: restaurant browsing (AP)\n\n━━━ THE OBSERVABILITY LAYER ━━━\n\nLOGS:\n  Services emit structured JSON logs\n  Fluentd → Kafka → Elasticsearch → Kibana\n  Correlation IDs in every log line\n\nMETRICS:\n  Services expose Prometheus metrics\n  Prometheus scrapes → stores → Grafana visualizes\n  Golden signals tracked: latency, traffic, errors, saturation\n\nTRACES:\n  OpenTelemetry SDK in every service\n  Traces exported to Jaeger\n  End-to-end request tracing across all services\n\nALERTING:\n  AlertManager → PagerDuty → on-call engineer\n  Runbooks in Notion for every alert\n\n━━━ THE SECURITY LAYER ━━━\n\nINGRESS:\n  TLS 1.3 on all public endpoints\n  WAF (Web Application Firewall) for attack prevention\n  DDoS protection via Cloudflare\n\nAUTH:\n  JWT for user sessions\n  OAuth2 for third-party login\n  SAML for enterprise B2B\n  RBAC for authorization\n\nSERVICE-TO-SERVICE:\n  mTLS via Istio (automatic)\n  Service identities via SPIFFE\n  Zero trust networking\n\nSECRETS:\n  HashiCorp Vault / AWS Secrets Manager\n  Auto-rotation, audit logs\n  IAM roles for service authentication (no static credentials)\n\nCOMPLIANCE:\n  PCI DSS for payment data\n  GDPR for user data\n  Audit logs for everything sensitive\n\n━━━ THE DEPLOYMENT ━━━\n\nINFRASTRUCTURE:\n  AWS (or multi-cloud for disaster recovery)\n  Regional deployment: Mumbai (primary), Bangalore, Chennai\n  CDN: CloudFront or Cloudflare\n\nDEPLOYMENT:\n  GitOps via ArgoCD\n  CI/CD via GitHub Actions\n  Canary deployments via Istio\n  Blue-green for critical services\n\nAUTOSCALING:\n  Horizontal Pod Autoscaler (based on CPU, custom metrics)\n  Cluster Autoscaler (add nodes when pods need room)\n  Scheduled scaling for known traffic patterns (lunch rush, dinner rush)\n\n━━━ THE NUMBERS ━━━\n\n  Users: 10 million MAU\n  Restaurants: 500,000\n  Orders/day: 500,000\n  Peak orders/second: ~200\n  API requests/second: ~100,000\n  Services: 50+\n  Databases: PostgreSQL (sharded), Redis, MongoDB, Elasticsearch, ClickHouse\n  Kafka throughput: 500K messages/second\n  Data warehouse: 10+ TB\n  Engineers: 200\n\n━━━ THE TAKEAWAY ━━━\n\nThis is what 'production-grade distributed system' means.\n\nEvery pattern we've covered exists because of a REAL problem at scale. None of them are 'nice to have' — each one solves a specific pain that emerges when you go from 10K users to 10M.\n\nFor an SDE-2 interview: you don't need to implement all of this. You need to UNDERSTAND why each piece exists, what trade-offs it makes, and when to apply it.\n\nStart simple. Scale only when you must. But KNOW the full landscape so you can recognize what's needed when you get there.\n\nYou've now seen the complete picture. From a single `google.com` request to a full distributed system. Every concept, connected.",
    analogy:"🍕 Your food delivery empire is now a multinational corporation with 50 branches, sophisticated supply chain, security systems, health inspectors, PR department, and accountants. Every piece we've discussed — from the chef's intercom (gRPC) to the security camera (logs) to the corporate identity system (SAML) to the franchise HQ (data warehouse) — all working together. You started as one restaurant. You're now an empire. And every single pattern, protocol, and piece of infrastructure exists because at some point, the old way stopped working. You can't skip the evolution — but you can understand WHERE you are on that journey and what comes next."
  },
];

// UI Components
function Tag({label}){return <span style={{display:"inline-block",padding:"3px 10px",borderRadius:"100px",fontSize:"10.5px",fontWeight:700,letterSpacing:".5px",background:"rgba(255,255,255,.07)",color:"rgba(255,255,255,.65)",border:"1px solid rgba(255,255,255,.1)",marginRight:5,marginBottom:4,textTransform:"uppercase"}}>{label}</span>}

function Chain({actors,color}){return <div style={{display:"flex",alignItems:"center",flexWrap:"wrap",gap:4,marginTop:10,marginBottom:6}}>{actors.map((a,i)=><span key={i} style={{display:"flex",alignItems:"center",gap:4}}><span style={{padding:"3px 9px",borderRadius:6,fontSize:"10.5px",fontWeight:600,background:i===0?color+"20":"rgba(255,255,255,.04)",color:i===0?color:"rgba(255,255,255,.55)",border:`1px solid ${i===0?color+"40":"rgba(255,255,255,.07)"}`,whiteSpace:"nowrap"}}>{a}</span>{i<actors.length-1&&<span style={{color:"rgba(255,255,255,.2)",fontSize:11}}>→</span>}</span>)}</div>}

function App(){
  const[activeStep,setActiveStep]=useState(0);
  const[showDetail,setShowDetail]=useState(false);
  const[showAnalogy,setShowAnalogy]=useState(false);
  const[activeSection,setActiveSection]=useState("structures");
  const contentRef=useRef(null);
  const step=S[activeStep];
  const sectionSteps=S.filter(s=>s.section===activeSection);
  const curSec=SECTIONS.find(s=>s.id===activeSection);
  const gi=S.indexOf(step);
  const canP=gi>0,canN=gi<S.length-1;

  useEffect(()=>{setShowDetail(false);setShowAnalogy(false);if(contentRef.current)contentRef.current.scrollTop=0},[activeStep]);
  useEffect(()=>{const f=S.findIndex(s=>s.section===activeSection);if(f>=0)setActiveStep(f)},[activeSection]);

  const nav=(d)=>{const n=gi+d;if(n>=0&&n<S.length){setActiveSection(S[n].section);setActiveStep(S[n].id)}};

  return(
    <div style={{minHeight:"100vh",background:"#0A0D12",color:"#E6EDF3",fontFamily:"'IBM Plex Sans',-apple-system,sans-serif",display:"flex",flexDirection:"column"}}>
      <style>{`@import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700;800&family=IBM+Plex+Mono:wght@400;500;600&display=swap');*{box-sizing:border-box;margin:0;padding:0}::-webkit-scrollbar{width:4px}::-webkit-scrollbar-track{background:transparent}::-webkit-scrollbar-thumb{background:rgba(255,255,255,.12);border-radius:10px}`}</style>

      <div style={{padding:"14px 18px 10px",borderBottom:"1px solid rgba(255,255,255,.05)"}}>
        <div style={{fontSize:10,fontWeight:700,textTransform:"uppercase",letterSpacing:2,color:"rgba(255,255,255,.25)",marginBottom:3,fontFamily:"'IBM Plex Mono',monospace"}}>System Design — Part 8 (Final)</div>
        <div style={{fontSize:17,fontWeight:800,color:"#fff",lineHeight:1.3}}>Advanced Patterns: From Data Structures to Production</div>
        <div style={{fontSize:11.5,color:"rgba(255,255,255,.35)",marginTop:3}}>The complete toolkit for a production-grade distributed system</div>
      </div>

      <div style={{padding:"8px 18px",borderBottom:"1px solid rgba(255,255,255,.05)",overflowX:"auto"}}>
        <div style={{display:"flex",gap:3,minWidth:"fit-content"}}>
          {SECTIONS.map(sec=><button key={sec.id} onClick={()=>setActiveSection(sec.id)} style={{display:"flex",alignItems:"center",gap:4,padding:"6px 10px",borderRadius:7,border:activeSection===sec.id?`1.5px solid ${sec.color}50`:"1.5px solid transparent",background:activeSection===sec.id?sec.color+"14":"rgba(255,255,255,.025)",color:activeSection===sec.id?sec.color:"rgba(255,255,255,.35)",cursor:"pointer",fontSize:10.5,fontWeight:activeSection===sec.id?700:500,fontFamily:"'IBM Plex Sans',sans-serif",whiteSpace:"nowrap"}}><span style={{fontSize:11}}>{sec.icon}</span><span>{sec.label}</span></button>)}
        </div>
      </div>

      <div style={{padding:"7px 18px",borderBottom:"1px solid rgba(255,255,255,.04)",overflowX:"auto"}}>
        <div style={{display:"flex",gap:3,minWidth:"fit-content"}}>
          {sectionSteps.map(s=><button key={s.id} onClick={()=>setActiveStep(s.id)} style={{padding:"5px 9px",borderRadius:6,border:activeStep===s.id?`1px solid ${s.color}40`:"1px solid transparent",background:activeStep===s.id?s.color+"10":"transparent",color:activeStep===s.id?s.color:"rgba(255,255,255,.3)",cursor:"pointer",fontSize:10.5,fontWeight:activeStep===s.id?700:500,fontFamily:"'IBM Plex Sans',sans-serif",whiteSpace:"nowrap"}}>{s.icon} {s.phase}</button>)}
        </div>
      </div>

      <div style={{height:2,background:"rgba(255,255,255,.03)"}}><div style={{height:"100%",width:`${((gi+1)/S.length)*100}%`,background:`linear-gradient(90deg,${step.color}88,${step.color})`,transition:"all .4s ease"}}/></div>

      <div ref={contentRef} style={{flex:1,overflow:"auto",padding:"14px 18px 110px"}}>
        <div style={{marginBottom:12}}>
          <div style={{display:"flex",alignItems:"center",gap:7,marginBottom:7}}>
            <span style={{fontSize:9,fontWeight:800,color:curSec.color,background:curSec.color+"18",padding:"2px 7px",borderRadius:4,fontFamily:"'IBM Plex Mono',monospace",letterSpacing:1}}>{curSec.label.toUpperCase()}</span>
            <span style={{fontSize:9,fontWeight:700,color:"rgba(255,255,255,.25)",fontFamily:"'IBM Plex Mono',monospace"}}>{gi+1} / {S.length}</span>
          </div>
          <h2 style={{fontSize:19,fontWeight:800,color:"#fff",lineHeight:1.3,marginBottom:7}}>{step.icon} {step.title}</h2>
          <div style={{display:"flex",flexWrap:"wrap",gap:2}}>{step.concepts.map((c,i)=><Tag key={i} label={c}/>)}</div>
        </div>
        <div style={{fontSize:9.5,fontWeight:700,textTransform:"uppercase",letterSpacing:1,color:"rgba(255,255,255,.22)",marginBottom:1}}>Who's involved</div>
        <Chain actors={step.actors} color={step.color}/>
        <div style={{background:"rgba(255,255,255,.025)",borderRadius:11,padding:15,border:"1px solid rgba(255,255,255,.055)",marginTop:12,fontSize:13.5,lineHeight:1.75,color:"rgba(255,255,255,.78)",whiteSpace:"pre-wrap"}}>{step.simple}</div>

        <button onClick={()=>setShowDetail(!showDetail)} style={{display:"flex",alignItems:"center",gap:7,width:"100%",padding:"11px 15px",marginTop:7,borderRadius:10,border:`1px solid ${step.color}30`,background:showDetail?step.color+"10":"transparent",color:step.color,cursor:"pointer",fontSize:12.5,fontWeight:700,fontFamily:"'IBM Plex Sans',sans-serif"}}>
          <span style={{transform:showDetail?"rotate(90deg)":"rotate(0deg)",transition:"transform .2s",fontSize:13}}>▶</span>{showDetail?"Hide":"Show"} Technical Deep Dive
        </button>
        {showDetail&&<div style={{background:"rgba(0,0,0,.3)",borderRadius:10,padding:16,border:`1px solid ${step.color}20`,marginTop:3,fontSize:12,lineHeight:1.85,color:"rgba(255,255,255,.7)",fontFamily:"'IBM Plex Mono',monospace",whiteSpace:"pre-wrap"}}>{step.detail}</div>}

        <button onClick={()=>setShowAnalogy(!showAnalogy)} style={{display:"flex",alignItems:"center",gap:7,width:"100%",padding:"11px 15px",marginTop:5,borderRadius:10,border:"1px solid rgba(255,255,255,.08)",background:showAnalogy?"rgba(255,255,255,.04)":"transparent",color:"rgba(255,255,255,.6)",cursor:"pointer",fontSize:12.5,fontWeight:700,fontFamily:"'IBM Plex Sans',sans-serif"}}>
          <span style={{transform:showAnalogy?"rotate(90deg)":"rotate(0deg)",transition:"transform .2s",fontSize:13}}>▶</span>{showAnalogy?"Hide":"Show"} Real-World Analogy
        </button>
        {showAnalogy&&<div style={{background:"rgba(255,255,255,.025)",borderRadius:10,padding:15,border:"1px solid rgba(255,255,255,.07)",marginTop:3,fontSize:13.5,lineHeight:1.75,color:"rgba(255,255,255,.6)"}}>{step.analogy}</div>}
      </div>

      <div style={{position:"fixed",bottom:0,left:0,right:0,padding:"10px 18px 16px",background:"linear-gradient(transparent,#0A0D12 30%)",display:"flex",gap:8}}>
        <button onClick={()=>nav(-1)} disabled={!canP} style={{flex:1,padding:12,borderRadius:10,border:"1px solid rgba(255,255,255,.08)",background:"rgba(255,255,255,.04)",color:canP?"rgba(255,255,255,.65)":"rgba(255,255,255,.18)",cursor:canP?"pointer":"default",fontSize:13,fontWeight:700,fontFamily:"'IBM Plex Sans',sans-serif"}}>← Back</button>
        <button onClick={()=>nav(1)} disabled={!canN} style={{flex:2,padding:12,borderRadius:10,border:"none",background:canN?`linear-gradient(135deg,${step.color},${step.color}99)`:"rgba(255,255,255,.08)",color:canN?"#fff":"rgba(255,255,255,.25)",cursor:canN?"pointer":"default",fontSize:13,fontWeight:700,fontFamily:"'IBM Plex Sans',sans-serif"}}>{canN?"Next →":"Journey complete! You know the full stack."}</button>
      </div>
    </div>
  );
}
