/*
 * DESIGN: CHESS GAME (hard)
 *
 * 45 MINUTES. SOLVE IT YOURSELF FIRST.
 *
 * Requirements:
 *   - 2 players (white, black) take turns
 *   - 8x8 board with 6 piece types: King, Queen, Rook, Bishop, Knight, Pawn
 *   - Each piece has its own movement rules
 *   - Capture: move to a cell occupied by opponent's piece
 *   - Check: king is under attack
 *   - Checkmate: king is in check and cannot escape → game over
 *   - Invalid move: violates piece rules, moves into check, wrong turn
 *
 * Your 7 questions:
 *   1. What can a user DO?        →
 *   2. What THINGS exist?         →
 *   3. What STATES exist?         →
 *   4. Who OWNS what?             →
 *   5. What can GO WRONG?         →
 *   6. What CHANGES at runtime?   →
 *   7. What if they ask to ADD?   →
 *
 * YOUR ENTITIES (fill in):
 *
 *
 * YOUR SERVICE METHODS (fill in):
 *
 *
 * CODE BELOW:
 */

public class ChessGame {
    public static void main(String[] args) {
        System.out.println("Chess Game - implement me!");
    }
}
