// Kth Element of Two Sorted Arrays (GFG)
// Difficulty: Hard | Priority: P1
// kth smallest in union of two sorted arrays (1-indexed).
// Example: Partition A and B so left has k elements and max(left) <= min(right).
// Approach: Binary search smaller array cut position.
// Time: O(log(min(m,n))), Space: O(1)

class Solution {
    public int kthElement(int[] a, int[] b, int k) {
        // TODO: Implement
    }
}
